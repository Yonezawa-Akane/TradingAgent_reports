# 新报告归档 Tour

用户说“处理一下新 report”“归档新报告”等时，执行以下流程。

源目录：`/Users/biankar/Documents/Codex/TradingAgents/results`

目标：本仓库的 `<TICKER>/<YYYY-MM-DD>/reports/`，目录和文件说明见 [README.zh-CN.md](README.zh-CN.md)。

1. 检查源目录的 `<TICKER>/<YYYY-MM-DD>/reports/`。源目录不存在或没有报告时，说明情况并结束。
2. 核对 ticker、研究日期和 README 列出的 7 份非空 Markdown 报告。名称无法识别、报告明示的研究日期与目录日期不一致、文件缺失或仍在写入的目录跳过并说明原因，其余继续处理。
3. 记录待复制报告的文件清单与逐文件 SHA-256，仅复制这 7 份报告，保持文件名和正文原样。运行日志、缓存和系统临时文件留在源目录。
4. 目标已存在时，比较文件清单和逐文件 SHA-256。完全一致则跳过；内容不同则先检查同日历史版本，已存在相同版本也跳过，否则以本次归档的本地时间追加 `_HHMMSS` 保存新版本。路径冲突时保留已有文件并说明原因，不能覆盖历史报告。
5. 复制后核对目标文件清单与逐文件 SHA-256，并重新检查源文件，确认与复制前一致。全部一致才算已归档；源文件变化或校验失败时移除本次新建的副本并报告失败，保留已有历史文件。
6. 汇报新增、已存在和跳过的报告目录数量及原因。源 `results` 目录及其全部内容保留，供 TradingAgents 继续使用。

此流程只做本地归档；commit、push 和 PR 需用户另行要求。README 不维护研究索引。
